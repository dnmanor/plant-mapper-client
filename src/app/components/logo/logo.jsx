import React from 'react'

const Logo = () => {

    const name = 'plant mapper'

    return (
        <div>
            <p className="logo" style={{textTransform:'uppercase'}}> {name}</p>
        </div>
    )
}

export default Logo
